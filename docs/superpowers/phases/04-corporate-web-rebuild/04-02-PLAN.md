---
phase: 04-corporate-web-rebuild
plan: 02
type: execute
---

<objective>
Full MCP tool re-test of all graph-dependent tools after corporate web expansion. Verify no regressions across housing, transaction, influence, and contractor graphs. Document final Phase 4 stats.

Purpose: Corporate web expansion touches the most interconnected part of the graph — corps link to owners (housing), transactions (ACRIS), political donations (influence), and enforcement (OATH). A regression in any downstream graph would be caught here.
Output: All MCP tools passing. Phase 4 complete.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-corporate-web-rebuild/04-01-SUMMARY.md

**Server**: Hetzner 178.156.228.119, container `common-ground-duckdb-server-1`
**SSH**: `ssh -i ~/.ssh/id_ed25519_hetzner fattie@178.156.228.119`

**Depends on 04-01**: graph_corps expanded, downstream tables rebuilt.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full MCP tool re-test (12 tools)</name>
  <files>None (testing only)</files>
  <action>
Test all 12 graph-dependent MCP tools:

**Corporate web tools:**
1. `corporate_web("Perlbinder")` — corp network
2. `shell_detector()` — WCC shell company detection
3. `llc_piercer("some new LLC")` — pierce corporate veil
4. `contractor_network("some contractor")` — contractor footprint

**Housing tools:**
5. `worst_landlords()` — ranked landlords
6. `landlord_network("1000670001")` — ownership network
7. `ownership_clusters()` — WCC building clusters
8. `building_profile("1000670001")` — building dossier

**Other graph tools:**
9. `property_history("1000670001")` — transaction history
10. `entity_xray("Barton Perlbinder")` — cross-domain
11. `flipper_detector()` — transaction flippers
12. `pay_to_play("Catsimatidis")` — political influence

Record pass/fail. Fix any failures immediately via SSH + docker cp + restart.

Collect final Phase 4 stats:
- All graph table counts (corps, people, edges, shared, buildings, owners, etc.)
  </action>
  <verify>All 12 tools return data without errors. No crashes, no regressions.</verify>
  <done>All 12 MCP tools pass. Phase 4 complete — corporate web expanded and verified.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 12 MCP tools tested and passing
- [ ] No regressions in any graph domain
- [ ] Phase 4 complete
</verification>

<success_criteria>

- All 12 MCP tools pass
- Phase 4 complete — ready for Phase 5 (Transaction Network Expansion)
</success_criteria>

<output>
After completion, create `.planning/phases/04-corporate-web-rebuild/04-02-SUMMARY.md`
</output>
