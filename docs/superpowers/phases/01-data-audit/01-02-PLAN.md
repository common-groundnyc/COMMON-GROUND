---
phase: 01-data-audit
plan: 02
type: execute
---

<objective>
Cross-reference the entity registry from Plan 01 against current graph table coverage. Identify every gap — tables with entity data that aren't feeding any graph. Produce the definitive expansion plan that drives Phases 3-6.

Purpose: This is the intelligence document that tells us exactly what to build in each subsequent phase.
Output: `.planning/phases/01-data-audit/graph_expansion_plan.md` — per-graph recommendations with specific tables, columns, and expected scale.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-audit/01-01-SUMMARY.md
@.planning/phases/01-data-audit/entity_registry.md

**Current graph tables** (from PROJECT.md audit):
- graph_owners: 194K (from hpd_jurisdiction + hpd_registration_contacts)
- graph_buildings: 348K (from hpd_jurisdiction only)
- graph_corps: 417K/104K unique (from nys_corporations filtered to HPD/PLUTO matches)
- graph_tx_entities: 1.96M (from acris_parties)
- graph_pol_entities: 311K (from campaign_contributions + campaign_expenditures)
- graph_contractors: 58K (from dob_permit_issuance)

**v1.0 resolved_entities**: 55.5M rows, 33.1M clusters across 44 tables — available as cross-reference backbone.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map current graph coverage gaps</name>
  <files>Output: analysis passed to Task 2</files>
  <action>
For each current graph, compare what tables feed it vs what tables COULD feed it based on entity_registry.md.

Query the live lake to verify:

1. **Housing graph** (graph_owners, graph_buildings):
   - Current sources: hpd_jurisdiction, hpd_registration_contacts
   - Check: What other tables have BBL columns? (PLUTO, DOB permits, FDNY, tax records, ACRIS legals, evictions, etc.)
   - Check: What other tables have owner names? (ACRIS parties, DOB application owners, PLUTO ownername, rolling_sales)

2. **Corporate web** (graph_corps):
   - Current sources: nys_corporations filtered to HPD/PLUTO name matches
   - Check: What other tables reference corporation names or dos_ids? (ACRIS parties, campaign contributions as employers, OATH hearings, doing_business, SBS certifications, DOB application owners)

3. **Transaction network** (graph_tx_entities):
   - Current sources: acris_parties + acris_master
   - Check: Are personal property tables (acris_pp_*) included? How many additional entities from pp tables?

4. **Influence network** (graph_pol_entities):
   - Current sources: campaign_contributions + campaign_expenditures
   - Check: What about cfb_offyear_contributions, doing_business, lobbying tables?

5. **NOT in any graph yet** — identify tables with rich entity data that aren't connected to any graph:
   - 311 service requests (21M rows with addresses)
   - OATH hearings (21.6M rows with respondent names + BBLs)
   - Citywide payroll (if it exists)
   - EPA ECHO facilities
   - Marriage index (4.7M)
   - Education tables (schools, enrollment)

Run SQL queries to quantify the gap:
```sql
-- Example: How many unique BBLs exist in tables NOT currently in graph_buildings?
SELECT 'pluto' as source, COUNT(DISTINCT bbl) FROM lake.city_government.pluto WHERE bbl IS NOT NULL
UNION ALL
SELECT 'dob_violations', COUNT(DISTINCT (boro || LPAD(block::VARCHAR, 5, '0') || LPAD(lot::VARCHAR, 4, '0')))
FROM lake.housing.dob_violations WHERE boro IS NOT NULL
-- etc.
```
  </action>
  <verify>Coverage gaps quantified for all 5 current graphs. Ungraphed tables identified with row counts.</verify>
  <done>Complete gap analysis showing current vs potential coverage for every graph, with specific table/column references.</done>
</task>

<task type="auto">
  <name>Task 2: Produce graph expansion plan</name>
  <files>.planning/phases/01-data-audit/graph_expansion_plan.md</files>
  <action>
Synthesize the gap analysis into a concrete expansion plan organized by target graph (maps directly to Phases 3-6).

For each graph, document:

**Recommended additions**:
| Table | Columns | Rows | Entity Type | Expected New Nodes | Priority |
|-------|---------|------|-------------|-------------------|----------|

**Memory estimate**: Calculate CSR memory for expanded graph (16 bytes/edge + 8 bytes/vertex).

**Cross-reference potential**: Which expanded graphs share entities via resolved_entities clusters?

**Phase mapping**:
- Phase 3 (Ownership): [specific tables to add]
- Phase 4 (Corporate): [specific tables to add]
- Phase 5 (Transaction): [specific tables to add]
- Phase 6 (Influence): [specific tables to add]
- Phase 7 (Cross-domain): [which entity types bridge which graphs]

**NEW graph candidates**: Should any entirely new graphs be created? E.g.:
- Enforcement graph (OATH + DOB violations + FDNY + HPD complaints per BBL)
- Education graph (schools + enrollment + test scores by district)
- Health graph (restaurants + inspections + complaints by location)

**resolved_entities integration**: For each graph, how does the Splink cluster_id connect entities across tables?

Mark each recommendation with priority (P0 = launch blocker, P1 = high value, P2 = nice to have).
  </action>
  <verify>Expansion plan covers all 5 existing graphs. Memory estimates within 28GB limit. Phase mapping is specific enough to execute.</verify>
  <done>graph_expansion_plan.md exists with per-graph recommendations, memory estimates, phase mapping, and priority rankings.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Gap analysis covers all current graphs
- [ ] Ungraphed tables identified
- [ ] Memory estimates calculated
- [ ] Phase mapping specific enough to write Phase 3-6 PLANs
- [ ] graph_expansion_plan.md written
- [ ] Phase 1 complete — both plans done
</verification>

<success_criteria>

- All tasks completed
- graph_expansion_plan.md is the definitive intelligence document for Phases 3-6
- Memory estimates confirm expansion fits within 28GB
- Cross-reference potential identified for Phase 7
- Phase 1 complete
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-audit/01-02-SUMMARY.md`:

# Phase 1 Plan 2: Entity Coverage Report Summary

**[One-liner about coverage gaps and expansion potential]**

## Accomplishments
- [Key gap findings]
- [New graph candidates]

## Files Created/Modified
- `.planning/phases/01-data-audit/graph_expansion_plan.md`

## Decisions Made
[Priority rankings, new graph recommendations]

## Issues Encountered
[Any data quality issues discovered]

## Next Phase Readiness
Phase 1 complete. Phase 2 (Bug Fixes) can proceed independently.
Phases 3-6 should reference graph_expansion_plan.md for specific table/column targets.
</output>
