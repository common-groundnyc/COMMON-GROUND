---
phase: 07-cross-domain-graph
plan: 02
type: execute
---

<objective>
Full verification of the unified graph and all MCP tools. Test cross-domain MATCH queries, verify entity_xray shows multi-domain connections, ensure no regressions across all 12+ tools.

Purpose: The unified graph is the culmination of Phases 3-6. Every domain graph must work independently AND cross-domain queries must work via the bridge tables.
Output: All tools verified, Phase 7 complete, ready for Phase 8 (PageRank).
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cross-domain-graph/07-01-SUMMARY.md

**Server**: Hetzner 178.156.228.119
**Depends on 07-01**: Bridge tables + unified property graph.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full MCP tool re-test + cross-domain verification</name>
  <files>None (testing only)</files>
  <action>
Test all graph-dependent MCP tools + cross-domain queries:

**Cross-domain tests (new):**
1. `sql_query("SELECT COUNT(*) FROM main.graph_unified_entities WHERE domain_count >= 3")` — entities in 3+ domains
2. `sql_query("SELECT * FROM main.graph_unified_entities ORDER BY domain_count DESC LIMIT 10")` — most connected cross-domain entities
3. Pick a multi-domain entity and test `entity_xray(name)` — should show connections across domains

**All domain tools (regression check):**
4. `worst_landlords()` — housing
5. `building_profile("1000670001")` — building
6. `landlord_network("1000670001")` — ownership
7. `ownership_clusters()` — WCC
8. `corporate_web("Perlbinder")` — corporate
9. `shell_detector()` — corporate WCC
10. `property_history("1000670001")` — transactions
11. `flipper_detector()` — flippers
12. `pay_to_play("Catsimatidis")` — influence
13. `entity_xray("Barton Perlbinder")` — cross-domain

Record pass/fail. Fix failures immediately.

Collect comprehensive stats for all graph tables.
  </action>
  <verify>All 13 tests pass. Cross-domain entities discoverable. No regressions.</verify>
  <done>All tools pass. Phase 7 complete — unified cross-domain graph operational.</done>
</task>

</tasks>

<verification>
- [ ] Multi-domain entities discoverable (3+ domains)
- [ ] All 13 tests pass
- [ ] Phase 7 complete
</verification>

<success_criteria>

- Cross-domain queries work
- All MCP tools pass
- Phase 7 complete — ready for Phase 8 (PageRank)
</success_criteria>

<output>
After completion, create `.planning/phases/07-cross-domain-graph/07-02-SUMMARY.md`
</output>
