---
phase: 02-universal-name-extractor
type: execute
---

<objective>
Fix name extraction quality across all 47 tables by adding parse-order awareness to COMBINED_SPACE pattern and validating extraction output.

Purpose: The current `get_extraction_sql()` assumes all COMBINED_SPACE tables use "FIRSTNAME LASTNAME" format, but ~8 tables actually use "LASTNAME FIRSTNAME" format. Without this fix, those tables produce swapped first/last names, breaking Splink matching in Phase 4.
Output: Corrected name_registry.py with per-table parse order, validated extraction across all 47 tables.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-name-column-discovery/01-01-SUMMARY.md

# Key files:
@src/dagster_pipeline/sources/name_registry.py
@src/dagster_pipeline/sources/entity_resolution.py

# Tech available: DuckDB MCP (mcp__duckdb__sql_query), Python dataclasses, DuckDB SQL
# Established patterns: NameSource dataclass, get_extraction_sql() per pattern type

# Constraining decisions:
# - Phase 1: 6 pattern types (STRUCTURED, COMBINED_COMMA, COMBINED_SPACE, OWNER, RESPONDENT, MULTI)
# - Phase 1: get_extraction_sql() returns list[str] of SQL per NameSource
# - Phase 1: LLC_FILTER_TERMS for corporate entity exclusion

# Known bugs discovered during planning:
# - COMBINED_SPACE always does SPLIT_PART(col, ' ', -1) for last_name — WRONG for "LASTNAME FIRSTNAME" tables
# - Affected tables: nys_cosmetology_licenses, nys_re_brokers, nys_re_appraisers, civil_list, nys_daily_corp_filings
# - nys_notaries is classified COMBINED_SPACE but actually uses comma format sometimes
# - nys_re_brokers and nys_child_care note "needs LLC filter" but COMBINED_SPACE handler doesn't apply it
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add parse order to NameSource and fix extraction SQL</name>
  <files>src/dagster_pipeline/sources/name_registry.py</files>
  <action>
  1. Add `last_name_first: bool = False` field to NameSource dataclass — when True, COMBINED_SPACE uses first word as last_name instead of last word.

  2. Add `needs_llc_filter: bool = False` field to NameSource — when True, COMBINED_SPACE applies LLC_FILTER_TERMS exclusion.

  3. Update these COMBINED_SPACE entries to set `last_name_first=True`:
     - nys_cosmetology_licenses (confirmed: "OLIVENCIA JACQUELINE M")
     - nys_re_brokers (confirmed: "TALUKDER MOHAMMED R")
     - nys_re_appraisers (notes say "LASTNAME FIRSTNAME MI")
     - civil_list (confirmed: "L E JONES" — initial + initial + LASTNAME, but first token approach at least gets last name closer)
     - nys_daily_corp_filings (check data first to confirm format)

  4. Update these to set `needs_llc_filter=True`:
     - nys_re_brokers (notes say "Mixed person/business names")
     - nys_child_care (notes say "Person names for home providers, org names for centers")

  5. Fix nys_notaries: Check live data. If mostly "FIRSTNAME LASTNAME" format (no comma), keep as COMBINED_SPACE with last_name_first=False. If mostly "LASTNAME, FIRSTNAME", reclassify as COMBINED_COMMA.

  6. Update `get_extraction_sql()` COMBINED_SPACE branch:
     - If `source.last_name_first`: `SPLIT_PART(col, ' ', 1)` for last_name, `SPLIT_PART(col, ' ', 2)` for first_name
     - If not: existing logic (`SPLIT_PART(col, ' ', -1)` for last_name, `SPLIT_PART(col, ' ', 1)` for first_name)
     - If `source.needs_llc_filter`: add LLC_FILTER_TERMS WHERE clause
  </action>
  <verify>
  Run via DuckDB MCP:
  ```sql
  -- Verify nys_cosmetology_licenses now extracts correctly
  SELECT UPPER(TRIM(SPLIT_PART(license_holder_name, ' ', 1))) AS last_name,
         UPPER(TRIM(SPLIT_PART(license_holder_name, ' ', 2))) AS first_name
  FROM lake.financial.nys_cosmetology_licenses
  WHERE license_holder_name IS NOT NULL LIMIT 5
  ```
  Should show: OLIVENCIA/JACQUELINE, not M/OLIVENCIA.

  Syntax check: `python3 -c "import ast; ast.parse(open('src/dagster_pipeline/sources/name_registry.py').read()); print('OK')"`
  </verify>
  <done>All COMBINED_SPACE entries have correct last_name_first flag. LLC filter applied where noted. Syntax check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Validate extraction quality across all 47 tables</name>
  <files>src/dagster_pipeline/sources/name_registry.py</files>
  <action>
  For every entry in NAME_REGISTRY, run `get_extraction_sql()` → take the generated SQL, add LIMIT 5, execute against the live lake via DuckDB MCP. Check:

  1. last_name looks like a last name (not an initial, not a first name, not "LLC")
  2. first_name looks like a first name (not swapped with last_name)
  3. No crashes on NULL or empty values
  4. Address columns produce valid data where available

  Do this in batches (10 tables at a time) to avoid overwhelming MCP.

  For any table where extraction looks wrong:
  - Check raw data (SELECT name_col LIMIT 10)
  - Fix the registry entry (pattern, last_name_first, name_columns)
  - Re-validate

  Track results: table name, pattern, sample output, pass/fail.
  </action>
  <verify>All 47 entries produce sensible (last_name, first_name) pairs when sampled. Print final pass/fail count.</verify>
  <done>47/47 tables validated. Any fixes documented in commit message.</done>
</task>

<task type="auto">
  <name>Task 3: Integration test — full UNION ALL extraction</name>
  <files>src/dagster_pipeline/sources/entity_resolution.py</files>
  <action>
  Run a count-only test of the full extraction pipeline:

  1. Generate all SQL from `get_extraction_sql()` for every registry entry
  2. Build the UNION ALL query (same as `_build_name_index` does)
  3. Run `SELECT source_table, COUNT(*) FROM (...) GROUP BY source_table ORDER BY 2 DESC LIMIT 20` against the lake
  4. Verify total row count is in the expected range (50M-70M raw, before 2+ table filter)
  5. Spot-check: top tables by count should be acris_parties, citywide_payroll, campaign_contributions

  No code changes expected in entity_resolution.py — this task validates that the registry + extractor work end-to-end. If issues found, fix in name_registry.py.
  </action>
  <verify>
  UNION ALL query executes without errors. Row counts per source_table are reasonable (no table with 0 rows, no table with suspiciously high counts from bad parsing).
  </verify>
  <done>Full extraction pipeline runs. Row counts documented. No errors.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All COMBINED_SPACE tables extract names in correct order (last_name_first where needed)
- [ ] LLC filter applied to COMBINED_SPACE tables that mix person/business names
- [ ] All 47 tables validated with LIMIT 5 sample
- [ ] Full UNION ALL extraction runs without errors
- [ ] `python3 -c "import ast; ast.parse(open('src/dagster_pipeline/sources/name_registry.py').read())"` passes
</verification>

<success_criteria>

- All tasks completed
- name_registry.py updated with last_name_first and needs_llc_filter fields
- All 47 tables produce correct (last_name, first_name) extraction
- Full extraction pipeline validated against live lake
- No import or syntax errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-universal-name-extractor/02-01-SUMMARY.md`
</output>
